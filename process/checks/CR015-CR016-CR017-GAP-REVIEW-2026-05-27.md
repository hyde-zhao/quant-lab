# CR-015 / CR-016 / CR-017 缺口检查

| 字段 | 值 |
|---|---|
| 检查时间 | 2026-05-27T22:33:51+08:00 |
| 检查范围 | `process/changes/CR-015-*.md`、`process/changes/CR-016-*.md`、`process/changes/CR-017-*.md` |
| 检查结论 | `PASS-WITH-PENDING-STANDARD-GATES` |
| 说明 | 三份 CR 已覆盖当前已知需求边界；仍需在 CP2/CP3/CP4/CP5 中补齐正式需求、HLD/ADR、Story Plan、LLD 和实现级验收。 |

## 总体结论

CR-015、CR-016 和 CR-017 当前都处于 **intake / change-impact-analysis** 层，不是实现批准。检查后已补齐两个关键缺口：

- CR-015 / CR-016 已补充“研究复权口径与 QMT 交易 raw 价格隔离”的交叉约束。
- CR-017 已作为独立高风险 standard CR 写入，覆盖 raw/qfq/hfq/returns_adjusted、qfq as-of、reader gate、QMT 执行价格和公司行动审计边界。

## 分 CR 检查结果

| CR | 已覆盖内容 | 本次发现缺口 | 本次处理 | 剩余待处理 |
|---|---|---|---|---|
| CR-015 | QMT adapter、OMS、order state machine、pre-trade risk、broker lake、shadow/mock 默认边界 | 缺少研究复权口径与真实交易价格隔离；备选方案优劣不够显式 | 已补 `QMT-D23`、备选方案摘要、CR-017 关联、禁止复权价下单 | CP2 需求增量、CP3 QMT HLD/ADR、CP5 LLD、QMT 官方 API spike、mock adapter 测试 |
| CR-016 | shadow -> 模拟盘 -> 实盘只读 -> 小资金实盘 -> 放大资金、runbook、对账、kill switch、资金放大门槛 | 缺少 CR-017 对真实策略激活和生产级声明的前置关系；备选方案优劣不够显式 | 已补 `QMT-D24`、备选方案摘要、CR-017 关联、禁止未声明复权口径进入实盘准入 | CR-015 CP7 前不得真实发单；还需 runbook、per-run 授权模板、kill switch 验收和对账阈值 |
| CR-017 | raw/qfq/hfq/returns_adjusted 分层、qfq as-of、hfq 研究默认、图表 qfq、QMT raw 执行、公司行动声明边界 | 新 CR 尚无 HLD/ADR、Story Plan、LLD、实现和测试 | 已创建 CR，并补充决策、备选方案、五维影响、LLD 批次门禁和禁止事项 | 需要 CP2/CP3 冻结公式、provider 字段解释、schema、reader API、migration 和回归测试 |

## 待决策 / 待冻结问题

| ID | 问题 | 推荐 | 不处理影响 | 后续门控 |
|---|---|---|---|---|
| GAP-1 | qfq/hfq 精确公式和 Tushare 字段解释是否在 HLD/ADR 阶段冻结 | 冻结后再实现 | 公式方向错误会污染历史价格、收益和因子 | CR-017 CP3 |
| GAP-2 | qfq 是否必须记录 `as_of_trade_date` | 必须记录 | 前复权历史价格漂移不可审计 | CR-017 CP3 / CP5 |
| GAP-3 | 旧 `prices` / `qfq` 报告如何迁移 | 保留旧基线，新视图增量发布 | 直接覆盖会破坏历史报告复现 | CR-017 CP5 |
| GAP-4 | QMT order intent 是否强制记录 `research_adjustment_policy` 和 `execution_price_policy` | 强制记录 | 研究口径和交易价格混淆，真实交易风险上升 | CR-015 CP5 |
| GAP-5 | CR-017 是否阻断 QMT 技术模拟盘 | 不阻断 broker 技术链路；阻断生产策略复权治理声明和资金放大 | 若完全阻断会拖慢 QMT adapter 验证；若完全不阻断会扩大研究口径风险 | CR-016 CP3 / CP5 |

## 当前禁止事项复核

| 禁止事项 | 覆盖状态 | 覆盖位置 |
|---|---|---|
| 未授权真实 QMT 发单 / 撤单 / 账户写操作 | 已覆盖 | CR-015、CR-016 |
| 未授权读取或保存 QMT 凭据 / 账户敏感信息 | 已覆盖 | CR-015、CR-016 |
| 未授权真实抓取、写湖、发布 current pointer | 已覆盖 | CR-014、CR-017 |
| 未授权把 qfq/hfq 复权价作为真实委托价或成交价 | 已覆盖 | CR-015、CR-016、CR-017 |
| 未授权声明完整公司行动审计 | 已覆盖 | CR-017 |
| 未经 CP5 批次确认不得实现 CR-017 | 已覆盖 | CR-017 |

## 建议推进顺序

| 顺序 | 范围 | 目的 |
|---|---|---|
| 1 | CR-017 CP2/CP3 | 先冻结复权公式、dataset/view 边界和研究/交易价格隔离，避免后续 QMT 使用错误价格口径。 |
| 2 | CR-015 CP2/CP3/CP5 | 完成 QMT foundation 的 adapter、OMS、risk、broker lake 设计和无真实发单实现。 |
| 3 | CR-017 CP5/实现/验证 | 在数据湖实现 raw/qfq/hfq/returns_adjusted 和 reader gate。 |
| 4 | CR-016 CP2/CP3/CP5 | 在 CR-015 foundation 和 CR-017 口径边界明确后设计模拟盘 / 实盘激活。 |

## 检查结论

当前没有发现需要立即重写 CR-015 或 CR-016 主体范围的缺口；已补齐与 CR-017 相关的交叉约束。三份 CR 的剩余工作都属于 standard 流程下的后续正式产物，不应在当前 CR intake 阶段直接实现。
