---
handoff_id: "META-SE-CR020-HLD-2026-06-04"
change_id: "CR-020"
target_agent: "meta-se"
source_agent: "meta-po"
status: "agent-completed-closed"
created_at: "2026-06-04T22:53:33+08:00"
updated_at: "2026-06-04T23:50:59+08:00"
phase: "solution-design"
dispatch:
  required: true
  semantic: "stage-dispatch"
  mode: "subagent"
  platform: "codex"
  agent_role: "meta-se"
  agent_path: ".agents/agents/meta-se.md"
  agent_id: "019e9341-e1c1-7d91-9432-2df92264f1dc"
  agent_name: "se-han"
  thread_id: "019e9341-e1c1-7d91-9432-2df92264f1dc"
  tool_name: "multi_agent_v1.spawn_agent"
  spawned_at: "2026-06-04T23:30:39+08:00"
  resumed_at: ""
  completed_at: "2026-06-04T23:32:09+08:00"
  closed_at: "2026-06-04T23:50:59+08:00"
  evidence: "spawn_agent returned agent_id=019e9341-e1c1-7d91-9432-2df92264f1dc nickname=se-han; wait_agent confirmed completed; close_agent closed the completed thread. meta-se delivered process/HLD.md §36, CP3 discussion log/checkpoint, CP3 auto precheck PASS, and checkpoints/CP3-CR020-HLD-REVIEW.md."
  fallback_reason: ""
---

# META-SE CR-020 HLD Handoff

## 目标

为 CR-020 输出 HLD / ADR / Story Plan / Development Plan，并准备 CP3 人工审查稿。

CR-020 当前范围：QMT Windows Gateway 服务端登录与只读查询接口准入。最小闭环为：

1. Windows S 端 gateway 可启动、停止、health、回滚；正式命令面为 `uv run` 启动的 Typer Python CLI，PowerShell / CMD 只作为 `uv run` 宿主。
2. 服务端读取本地未跟踪 `.env`，完成 QMT / MiniQMT / XtQuant 登录和 session ready 检查。
3. Linux C 端使用 `uv run` Typer CLI 完成 pairing、诊断、smoke test 和 CP7 验收。
4. `local_backtest` 实际业务调用使用 Python REST client 直接调用 gateway REST API。
5. 首个查询接口为 `query_positions`，只读、脱敏、scope=`qmt:positions:read`。
6. HMAC / allowlist / scope / redaction / no-order safety 必须 fail-closed。

## 必读输入

| 类型 | 路径 | 用途 |
|---|---|---|
| 正式 CR | `process/changes/CR-020-QMT-WINDOWS-GATEWAY-SERVER-LOGIN-READONLY-QUERY-ADMISSION-2026-06-04.md` | 范围、验收、Story 候选和不授权边界 |
| CP2 自动预检 | `process/checks/CP2-CR020-REQUIREMENTS-BASELINE.md` | 自动检查结果 |
| CP2 人工审查 | `checkpoints/CP2-CR020-REQUIREMENTS-BASELINE.md` | 已 approved 的 7 项决策 |
| CP2 discussion log | `process/discussions/CP2-CR020-SCENARIO-DISCUSSION-LOG.md` | 用户澄清和灰区处理 |
| endpoint matrix | `trading/qmt_endpoint_matrix.py` | 现有 QMT endpoint / scope / later-gated 合同 |
| client contract | `trading/qmt_client.py` | C 端 offline / later-gated client 合同 |
| auth contract | `trading/qmt_auth.py` | HMAC / pairing 合同 |
| gateway lifecycle | `trading/qmt_gateway_config.py`、`trading/qmt_gateway_service.py` | 现有 gateway lifecycle 合同 |
| install doc | `docs/QMT-GATEWAY-INSTALL.md` | 当前离线安装边界，后续需修订 |

## CP2 已批准决策

| 决策 ID | 结论 |
|---|---|
| DQ-CP2-CR020-01 | CR-020 升级为 Windows Gateway 服务端登录 + 至少一个只读查询接口。 |
| DQ-CP2-CR020-02 | 授权 CR-020 门禁内的只读 runtime：gateway start / port bind / QMT server login / session check / readonly query。 |
| DQ-CP2-CR020-03 | 真实 QMT 账号和密码只进入本地未跟踪 `.env`；入库只允许 `.env.example` 占位变量和脱敏 `credential_ref`。 |
| DQ-CP2-CR020-04 | S 端 Windows 使用 `uv run` Typer CLI；C 端 Linux 使用 `uv run` Typer CLI 只用于配对 / 诊断 / 验收；实际业务调用由 Python REST client 调用 REST API。 |
| DQ-CP2-CR020-05 | 首个接口为 `query_positions`。 |
| DQ-CP2-CR020-06 | 接受真实 QMT 只读 API 风险；不授权任何交易或写操作。 |
| DQ-CP2-CR020-07 | gateway runtime 依赖隔离；CP3 前不修改 `pyproject.toml` / `uv.lock`。 |

## HLD 必须覆盖的问题

| 主题 | 必须回答 |
|---|---|
| Gateway 架构 | Windows gateway 进程边界、Typer CLI 命令组、`uv run` 启动 / 停止 / health、端口绑定、日志目录、回滚方式。 |
| 登录 / 会话 | `.env` 读取、QMT 登录、session ready、超时、重连、fail-closed、日志脱敏。 |
| S / C 调用 | S 端 Typer CLI 启停 / 诊断、C 端 Typer CLI pairing / diagnostics / validation 与 Python REST client runtime 的职责边界；两端 CLI 框架保持 Typer 一致性。 |
| 安全 | HMAC、allowlist、scope、timestamp / nonce、防重放、redaction、错误响应。 |
| 只读接口 | `query_positions` route、request / response schema、scope=`qmt:positions:read`、脱敏字段、blocked result。 |
| 依赖隔离 | Windows S 端 gateway / Typer / QMT 依赖、Linux C 端 Typer CLI 依赖与 Linux C 端业务 runtime 隔离，CP3 是否采用 optional group / extras / external runtime。 |
| 不授权边界 | 明确不授权发单、撤单、账户写入、simulation/live、provider/lake/publish、凭据泄露。 |
| Story 拆解 | 至少评估 CR020-S01..S06，并生成 Wave / file owner / CP5 批次建议。 |

## 推荐 Story 候选

| Story | 标题 |
|---|---|
| CR020-S01 | Windows gateway runtime 与部署准入 |
| CR020-S02 | 服务端 QMT 登录 / 会话管理 |
| CR020-S03 | C 侧 Python REST transport |
| CR020-S04 | HMAC / pairing / allowlist / scope 实机准入 |
| CR020-S05 | `query_positions` 只读查询接口 |
| CR020-S06 | 文档、运行手册与 CP7 实机验收 |

## 不授权项

CP3 设计通过仍不授权：

- 发单、撤单、改单、账户写入或任何 broker 写操作。
- simulation、live-readonly、small-live、scale-up 或任何真实交易准入。
- provider fetch、真实联网补数、真实 lake write、catalog publish / current pointer publish、broker lake write 或 reports overwrite。
- 把 `query_positions` 以外的真实 QMT 查询接口纳入本轮默认白名单。
- 把账号、密码、token、session、交易密码、私钥写入 Git、对话、日志、检查点、memory 或任何入库文件。
- 在 CP3 前修改 `pyproject.toml` / `uv.lock` 或把 Windows gateway 依赖加入 Linux C 端主依赖。
- 把 C 端 Typer CLI 配对 / 验收命令误当成实际业务 runtime 调用方式，或绕过 Python REST client 合同。

## 交付要求

meta-se 完成后应至少交付：

1. `process/HLD.md` 的 CR-020 HLD 增量章节。
2. `process/ARCHITECTURE-DECISION.md` 的 CR-020 ADR 增量章节。
3. `process/STORY-BACKLOG.md` 的 CR020-S01..S06 Story 增量。
4. `process/DEVELOPMENT-PLAN.yaml` 的 CR-020 Wave / file owner / dev gate 增量。
5. `process/discussions/CP3-CR020-HLD-DISCUSSION-LOG.md`。
6. `process/checks/CP3-CR020-DISCUSSION-CHECKPOINT.json`。
7. `process/checks/CP3-CR020-HLD-CONSISTENCY.md`。
8. `checkpoints/CP3-CR020-HLD-REVIEW.md`。

## 调度说明

本文件已由 meta-po 回填真实 `spawn_agent` / `wait_agent` / `close_agent` 证据。meta-se 已完成 CP3 HLD 设计并关闭线程；正式 ADR / Story Backlog / Development Plan 仍需 CP3 人工确认后再进入 story-planning 阶段生成。
