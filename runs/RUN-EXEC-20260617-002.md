---
run_exec_id: "RUN-EXEC-20260617-002"
workflow_id: "CR089-QMT-INTERFACE-VALIDATION"
status: "authorized"
executed_at: "2026-06-17T22:40:30+08:00"
operator: "user"
source: "user-authorization"
execution_scope: "runtime-smoke-query-positions-readonly"
runtime_authorized: true
---

# RUN-EXEC 记录

## 执行环境快照

| 项目 | 值 |
|---|---|
| 平台 | Windows 交易主机，用户手工执行 |
| 运行位置 | 由用户在交易主机本机选择；不得由 agent 远程执行 |
| 版本 / 提交 | CR089 `qmt_interface_smoke` runtime smoke |
| 关键依赖 | `trading.qmt_runtime_cli`、用户本机 QMT / gateway 配置、`scope=qmt:positions:read` |

## 任务执行结果

| 任务 / 步骤 | 结果（pass/fail/blocked/skipped） | actual_result | error_message | evidence |
|---|---|---|---|---|
| CR089 runtime smoke authorization | pass | 用户明确授权本次仅允许手工执行 `qmt_interface_smoke` 的 `query_positions` 只读验证，scope 为 `qmt:positions:read`。 | 无 | 当前对话 |
| Agent credential / account access | blocked | 用户明确不授权 agent 读取 `.env`、账号、密码、token、session 或 QMT 凭据，不授权输出账户、持仓、资金、委托、成交或日志原文。 | N/A | 当前对话 |
| Trading write operations | blocked | 用户明确不授权 `submit_order`、`cancel_order`、simulation、live。 | N/A | 当前对话 |

## 异常与偏差

- 本记录只登记 runtime smoke 授权，不包含任何账户、凭据、QMT 日志或持仓原文。
- 本授权仅覆盖用户本人在交易主机手工执行 `query_positions` 只读验证；不授权 agent 代跑命令。
- 本授权不改变 CR046 active formal CR 状态，不恢复 CR020，不授权 NAS publish / pull / list / copy。

## 用户补充说明

- 用户授权原文摘要：
  - 仅允许交易主机手工执行 `qmt_interface_smoke` 的 `query_positions` 只读验证。
  - scope 为 `qmt:positions:read`。
  - 不授权 agent 读取 `.env`、账号、密码、token、session 或 QMT 凭据。
  - 不授权输出账户、持仓、资金、委托、成交或日志原文。
  - 不授权 `submit_order`、`cancel_order`、simulation、live。
  - 结果只允许按 `redacted-smoke-result-template.yaml` 回填脱敏摘要。

## 关联问题

| ISSUE / CR | 关系 | 说明 |
|---|---|---|
| CR-089 | runtime-authorization | 本次 `query_positions` 只读 smoke 已获用户授权 |
| CR-046 | parent / active formal CR | 仍保持 `active-cp6-pass-ready-for-verification`，本记录不恢复 CP7 |
| CR-020 | historical reference only | 仍为 `deleted-by-user`，不得恢复为当前验证入口 |

## 后续门禁

| 下一步 | 状态 | 说明 |
|---|---|---|
| 用户手工运行 `server-diagnostics` | allowed-within-this-smoke | 输出必须保持 redacted，不贴 `.env` 原文 |
| 用户手工运行 gateway / QMT 必要组件 | allowed-only-if-required-for-query_positions | 仅为本次只读 smoke 服务，不得扩展 endpoint |
| 用户手工运行 `query-positions` | allowed | 只允许 `scope=qmt:positions:read`，只回填脱敏摘要 |
| 任何原始账户 / 持仓 / 委托 / 成交 / 日志输出 | blocked | 不得贴回或入库 |
| submit / cancel / simulation / live | blocked | 不属于本次授权 |
