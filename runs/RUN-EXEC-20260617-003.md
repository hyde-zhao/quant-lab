---
run_exec_id: "RUN-EXEC-20260617-003"
workflow_id: "CR089-QMT-INTERFACE-VALIDATION"
status: "pass"
executed_at: "2026-06-17T23:39:37+08:00"
operator: "user"
source: "user-feedback"
execution_scope: "runtime-smoke-server-diagnostics"
runtime_authorization_ref: "cr089-runtime-smoke-20260617-query-positions"
---

# RUN-EXEC 记录

## 执行环境快照

| 项目 | 值 |
|---|---|
| 平台 | Windows 交易主机，PowerShell，用户手工执行 |
| 运行位置 | `C:\Users\hydez\quant-lab-intake` |
| 版本 / 提交 | CR089 `qmt_interface_smoke` runtime smoke；本记录仅覆盖 server diagnostics |
| 关键依赖 | `uv run --with typer --python 3.11`、`trading.qmt_runtime_cli`、本机 `.env`、QMT / MiniQMT / XtQuant 本地配置 |

## 任务执行结果

| 任务 / 步骤 | 结果（pass/fail/blocked/skipped） | actual_result | error_message | evidence |
|---|---|---|---|---|
| CR089 server diagnostics | pass | CLI 返回 `status=ok`、`schema_version=cr020-runtime-manual-validation-v1`、`secrets_redacted=true`；`host=127.0.0.1`、`port=18765`、`allowed_source=127.0.0.1/32`；`runtime_authorization_ref=cr089-runtime-smoke-20260617-query-positions`。 | 无 | 用户反馈 |
| `.env` 配置加载 | pass | `client_secret_ref` 为 `[REDACTED]`；`client_id_hash` 存在但未入库；`account_ref` 为脱敏引用且未入库；`miniqmt_path_configured=true`；`xtquant_site_packages_configured=true`；`account_type=STOCK`。 | 无 | 用户反馈 |
| Gateway `serve` 启动 | skipped | 尚未执行；`server-diagnostics` 只验证配置构建，不启动 HTTP server，不懒加载 XtQuant，不连接 MiniQMT。 | N/A | 当前记录 |
| `query_positions` 只读查询 | skipped | 尚未执行；本记录不证明 QMT 持仓查询链路可用。 | N/A | 当前记录 |
| 原始账户 / 持仓 / 资金 / 委托 / 成交 / 日志输出 | blocked | 未授权输出，用户本次反馈未提供原文。 | N/A | 当前授权边界 |
| submit / cancel / simulation / live | blocked | 不属于本次授权范围。 | N/A | 当前授权边界 |

## 异常与偏差

- 本记录是 runtime smoke 的配置诊断 PASS，不是 QMT 接口查询 PASS。
- `server-diagnostics` 不触发 `xtquant` import、MiniQMT login、gateway 端口绑定或账户查询。
- 为避免长期可关联指纹，本记录不保存用户输出中的 `client_id_hash` 和 `account_ref` 具体哈希值。
- 本记录不改变 CR089 blocked 状态，不恢复 CR046 CP7，不恢复 CR020。

## 用户补充说明

- 用户在交易主机执行：

```powershell
uv run --with typer --python 3.11 python -m trading.qmt_runtime_cli server-diagnostics --env-file .env --host 127.0.0.1 --port 18765 --runtime-authorization-ref cr089-runtime-smoke-20260617-query-positions
```

- 用户反馈的 JSON 已脱敏，包含 `secrets_redacted=true`、`client_secret_ref=[REDACTED]`、脱敏 `account_ref`、`miniqmt_path_configured=true` 和 `xtquant_site_packages_configured=true`。

## 关联问题

| ISSUE / CR | 关系 | 说明 |
|---|---|---|
| CR-089 | execution-feedback | `server-diagnostics` 已通过，可继续进入 gateway `serve` 前置检查 |
| CR-046 | parent / active formal CR | 仍保持 `active-cp6-pass-ready-for-verification`，本记录不恢复 CP7 |
| CR-020 | historical reference only | 仍为 `deleted-by-user`，只复用 runtime CLI 代码和安全边界 |

## 后续门禁

| 下一步 | 状态 | 说明 |
|---|---|---|
| 用户手工启动 `serve` | allowed-within-this-smoke | 仅限本机 `127.0.0.1:18765`，仅为 `query_positions` 只读 smoke 服务 |
| 用户手工运行 `client-diagnostics` | allowed-within-this-smoke | 只验证本地 client 配置，不贴 `.env` 原文 |
| 用户手工运行 `query-positions` | allowed | 只允许 `scope=qmt:positions:read`，只回填脱敏摘要 |
| 任何原始账户 / 持仓 / 委托 / 成交 / 日志输出 | blocked | 不得贴回或入库 |
| submit / cancel / simulation / live | blocked | 不属于本次授权 |
