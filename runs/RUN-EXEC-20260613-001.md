---
run_exec_id: "RUN-EXEC-20260613-001"
workflow_id: "local_backtest-goldminer-simulation-precheck"
status: "recorded"
executed_at: "2026-06-13"
operator: "user"
source: "user-feedback"
---

# RUN-EXEC 记录

## 执行环境快照

| 项目 | 值 |
|---|---|
| 平台 | Windows |
| 运行位置 | Windows 掘金终端所在主机，`C:\quant\gm_api_check` 与本地项目运行目录 |
| 版本 / 提交 | 用户反馈未提供 Git commit；以本次对话和报告路径为证据入口 |
| 关键依赖 | `uv`、`gm`、`gmtrade`、掘金 Windows 终端、项目 `scripts/run_paper_simulation.py` |

## 任务执行结果

| 任务 / 步骤 | 结果（pass/fail/blocked/skipped） | actual_result | error_message | evidence |
|---|---|---|---|---|
| `gm` 行情 API token / terminal 校验 | pass | `get_trading_dates` 与 `history` 成功；`2026-06-01` 到 `2026-06-12` 返回 10 条交易日 / 日线 | N/A | 用户反馈：Windows 侧执行结果 |
| `gmtrade` 只读账户校验 | pass | 成功返回 Cash；持仓 0；未完成委托 0；成交回报 0 | N/A | 用户反馈：token/account_id 未打印，仅用于只读校验 |
| 多因子组合本地 paper simulation | pass | `pass`；11 个 order intents、11 个 fills、11 个 positions；reconciliation pass；forbidden counters 全 0 | N/A | `reports/paper_simulation/gm-api-paper-sim-mf-20240409-20260613/PAPER-SIMULATION-REPORT.json` |
| 低波动组合本地 paper simulation | pass | `pass`；20 个 order intents、20 个 fills、20 个 positions；reconciliation pass；forbidden counters 全 0 | N/A | `reports/paper_simulation/gm-api-paper-sim-low-vol-20240409-20260613/PAPER-SIMULATION-REPORT.json` |
| 掘金模拟 / 实盘下单 | skipped | 未执行真实下单、撤单或启动掘金仿真 / 实盘交易 | N/A | 用户反馈；`scripts/run_paper_simulation.py` 为本地 paper simulation runner |

## 异常与偏差

- 用户反馈中明确 `.env` 未打印，token/account_id 未作为证据输出，仅用于 Windows 本机只读校验。
- 本次 `gmtrade` 只读校验已超过 CR045 原先关闭范围；应作为后续 CR046 / runtime authorization 的输入证据，不回写为 CR045 已授权 L5。
- 本地 `run_paper_simulation.py` 输出中的 `simulation_ready=false`、`live_ready=false` 仍然有效；本次 paper simulation 不等于掘金仿真账户已开始交易。
- 报告路径位于 Windows / 用户运行环境；当前仓库内未直接读取报告内容，后续若作为正式 CP7 证据，应拷贝脱敏报告或提供 hash。

## 用户补充说明

- 已完成 Windows 主机验证。
- `.env` 没有被打印，token/account_id 只用于掘金只读校验。
- 行情 API：`get_trading_dates` 和 `history` 成功，`2026-06-01` 到 `2026-06-12` 返回 10 条交易日 / 日线。
- `gmtrade` 只读账户校验：成功返回 Cash，持仓 0，未完成委托 0，成交回报 0。
- 多因子组合本地 paper simulation：`pass`，11 个 order intents、11 个 fills、11 个 positions，reconciliation pass，forbidden counters 全 0。
- 低波动组合本地 paper simulation：`pass`，20 个 order intents、20 个 fills、20 个 positions，reconciliation pass，forbidden counters 全 0。
- 本地 paper simulation 没有真实下单、撤单或启动掘金仿真 / 实盘交易。

## 关联问题

| ISSUE / CR | 关系 | 说明 |
|---|---|---|
| CR-041 | evidence-consumer | 本地 paper simulation runner 已在 Windows 主机以用户反馈方式验证通过。 |
| CR-045 | follow-up-input | CR045 关闭范围仍是 L2 skeleton；本次 `gmtrade` 只读结果可作为后续 runtime gate 输入。 |
| CR-046 | candidate-input | 建议作为 Goldminer Simulation Account Runtime Runner 的启动输入，覆盖 L3 login / L4 readonly 预验证事实。 |
