---
run_exec_id: "RUN-EXEC-20260617-005"
workflow_id: "CR089-QMT-INTERFACE-VALIDATION"
status: "pass"
executed_at: "2026-06-17T23:47:57+08:00"
operator: "user"
source: "user-feedback"
execution_scope: "runtime-smoke-client-diagnostics-and-query-positions"
runtime_authorization_ref: "cr089-runtime-smoke-20260617-query-positions"
---

# RUN-EXEC 记录

## 执行环境快照

| 项目 | 值 |
|---|---|
| 平台 | Windows 交易主机，PowerShell，用户手工执行 |
| 运行位置 | `C:\Users\hydez\quant-lab-intake` |
| 版本 / 提交 | CR089 `qmt_interface_smoke` runtime smoke；本记录覆盖 client diagnostics 与 `query_positions` 只读查询 |
| 关键依赖 | `uv run --with typer --python 3.11`、`trading.qmt_runtime_cli`、本机 `.env`、本机 `127.0.0.1:18765` gateway、QMT / MiniQMT / XtQuant ready session |

## 任务执行结果

| 任务 / 步骤 | 结果（pass/fail/blocked/skipped） | actual_result | error_message | evidence |
|---|---|---|---|---|
| CR089 client diagnostics | pass | CLI 返回 `status=ok`、`base_url=http://127.0.0.1:18765`、`client_secret_ref=[REDACTED]`；`client_id_hash` 存在但未入库。 | 无 | 用户反馈 |
| `query_positions` 只读 smoke | pass | CLI 返回 `status=ok`、`endpoint=positions`、`reason_code` 为空、`blocked_result=null`；gateway result 为 `allowed=true`、`blocked=false`、`transport_status=allowed`、`status_code=200`。 | 无 | 用户反馈 |
| scope 与授权引用 | pass | 响应包含 `scope=qmt:positions:read`、`authorization_ref=cr089-runtime-smoke-20260617-query-positions`、`readonly_query_authorized=true`、`operation_authorized=false`、`real_operation=false`。 | 无 | 用户反馈 |
| 持仓摘要脱敏 | pass | `position_count_bucket=zero`、`positions_digest=positions:e025dfde5d22f9d1`、`redaction_status=pass`、`raw_payload_emitted=false`、`items_redacted=[]`。 | 无 | 用户反馈 |
| 只读查询计数 | pass | gateway counters 显示 `query_positions_read_attempt=1`、`readonly_positions_adapter_call=1`、`raw_positions_emit=0`、`redaction_fallback_to_raw=0`；部分敏感 counter 已脱敏。 | 无 | 用户反馈 |
| 禁止操作计数 | pass | client safety counters 显示 `real_order=0`、`real_cancel=0`、`account_write=0`、`provider_fetch=0`、`lake_write=0`、`publish=0`、`simulation_or_live_run=0`、`credential_read=0`。 | 无 | 用户反馈 |
| 原始账户 / 持仓 / 资金 / 委托 / 成交 / 日志输出 | blocked | 未授权输出，记录只保存脱敏摘要。 | N/A | 当前授权边界 |
| submit / cancel / simulation / live | blocked | 不属于本次授权范围，未执行。 | N/A | 当前授权边界 |

## 异常与偏差

- 用户本次粘贴了完整 JSON，其中包含 `client_id_hash`、`client_id_ref`、`nonce_ref`、`signature_ref` 等可关联引用；本记录不保存这些具体值。
- 本次 `query_positions` 返回 `position_count=0`，本记录只解释为本次只读查询返回 0 条持仓摘要，不推断账户资金或账户整体状态。
- `operation_authorized=false` 和 `real_operation=false` 表示本次只读 smoke 未授权交易写操作；本次 PASS 不表示 submit / cancel / simulation / live 可用。
- 本记录不改变 CR089 blocked 状态，不恢复 CR046 CP7，不恢复 CR020。

## 用户补充说明

- 用户在交易主机窗口 B 执行：

```powershell
uv run --with typer --python 3.11 python -m trading.qmt_runtime_cli client-diagnostics --env-file .env --base-url http://127.0.0.1:18765
uv run --with typer --python 3.11 python -m trading.qmt_runtime_cli query-positions --env-file .env --base-url http://127.0.0.1:18765 --run-id cr089-query-positions-20260617-001 --request-id cr089-query-positions-20260617-001 --timeout-seconds 10
```

- 用户反馈的 `query_positions` 摘要：
  - `status=ok`
  - `endpoint=positions`
  - `reason_code=` 空
  - `payload.query_positions.redaction_status=pass`
  - `payload.query_positions.position_count_bucket=zero`
  - `payload.query_positions.positions_digest=positions:e025dfde5d22f9d1`
  - `payload.query_positions.raw_payload_emitted=false`

## 关联问题

| ISSUE / CR | 关系 | 说明 |
|---|---|---|
| CR-089 | runtime-smoke-pass | `query_positions` 只读 smoke 已按授权范围通过 |
| CR-046 | parent / active formal CR | 仍保持 `active-cp6-pass-ready-for-verification`，本记录不恢复 CP7 |
| CR-020 | historical reference only | 仍为 `deleted-by-user`，只复用 runtime CLI 代码和安全边界 |

## 后续门禁

| 下一步 | 状态 | 说明 |
|---|---|---|
| 停止 PowerShell 窗口 A 的 `serve` | allowed | 本次 smoke 已完成；如不继续验证，可由用户本机 `Ctrl+C` 停止 |
| 追加真实账户 / 持仓 / 日志证据 | blocked | 不得贴回或入库 |
| 扩展 health / capabilities / submit / cancel / simulation / live | blocked | 必须另起独立高风险 gate |
| NAS package pull / publish / copy / delete | blocked | 不属于本次 runtime smoke 授权 |
